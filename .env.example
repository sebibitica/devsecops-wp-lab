# Copy this file to .env (gitignored) so docker-compose can read it locally.
#
#   cp .env.example .env
#
# DOCKERHUB_USERNAME — required. Your Docker Hub user.
# TAG                — optional. Image tag. Defaults to "latest".

DOCKERHUB_USERNAME=yourdockerhubuser
TAG=latest
